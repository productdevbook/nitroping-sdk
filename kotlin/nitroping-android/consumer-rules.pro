# Consumer ProGuard rules — empty for now. The SDK uses only stdlib
# classes + kotlinx.coroutines, both of which ship their own consumer
# rules. Keep this file so R8 doesn't warn about a missing reference if a
# consumer enables minification on a debug build.
